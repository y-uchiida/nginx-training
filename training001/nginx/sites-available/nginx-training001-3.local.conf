server{
    listen       80;
    listen  [::]:80;
    server_name  nginx-training001-3.local;

	# /var/www/public 内に作成したディレクトリをルートに設定
	location / {
		root /var/www/public/nginx-training001-3.local;
	}
}	